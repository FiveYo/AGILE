<Page
    x:Class="FastDelivery_IHM.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastDelivery_IHM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="0.3*" />
            <ColumnDefinition Width="0.7*" />
        </Grid.ColumnDefinitions>
        <Grid x:Name="mapGrid" Grid.Column="1" Margin="0,0,0,0">
            <Canvas x:Name="mapCanvas" HorizontalAlignment="Stretch" Margin="0,0,0,0" VerticalAlignment="Stretch"/>
        </Grid>
        <StackPanel HorizontalAlignment="Stretch"  Margin="0,0,0,0" VerticalAlignment="Stretch">
            <Grid Height="100">
                <Button x:Name="button" Content="Load Plan" HorizontalAlignment="Left" Margin="0,0,0,0" VerticalAlignment="Center" Click="button_Click"/>

            </Grid>
            <ListView x:Name="listLivraison" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Margin="0,0,0,0"/>
        </StackPanel>
    </Grid>
</Page>
