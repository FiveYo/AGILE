<UserControl 
    x:Name="livUI"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:FastDelivery_IHM"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Maps="using:Windows.UI.Xaml.Controls.Maps"
    x:Class="FastDelivery_IHM.Delivery"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <StackPanel Padding="10">
        <SplitView DisplayMode="Inline"  IsPaneOpen="{Binding displayCheck, ElementName=livUI}" 
               OpenPaneLength="20" PaneBackground="White">
            <SplitView.Pane>
                <CheckBox x:Name="checkbox" Width="20" Height="20" />
            </SplitView.Pane>
            <StackPanel Padding="10" VerticalAlignment="Center">
                <TextBlock HorizontalAlignment="Left" Margin="0" TextWrapping="Wrap" Text="{Binding description, ElementName=livUI}" />
            </StackPanel>
        </SplitView>
        <Grid x:Name="add">
            <Border Background="WhiteSmoke">
                <Button x:Name="addBtn" Content="Ajouter une livraison ici" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Background="{x:Null}" FontSize="10">
                </Button>
            </Border>
        </Grid>
    </StackPanel>
</UserControl>
